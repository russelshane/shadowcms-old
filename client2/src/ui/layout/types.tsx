/**
 * @description Types for Layout UI Component
 * @author ShadowCMS
 */

import { ReactNode } from "react";

type LayoutProps = {
  title?: string;
  children?: ReactNode;
};

export default LayoutProps;
