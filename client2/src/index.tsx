/**
 * @description Shadow
 * @author ShadowCMS
 */

import ReactDOM from "react-dom";
import Application from "./application";

/**
 * @description Render main application to react-root div element
 */
ReactDOM.render(<Application />, document.getElementById("react-root"));
